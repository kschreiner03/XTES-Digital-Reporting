import { defineConfig } from 'vite';

// https://vitejs.dev/config
export default defineConfig({
  // The custom resolve configuration has been removed to allow the
  // Electron Forge Vite plugin to correctly process the main entry file
  // and inject necessary development variables.
});
